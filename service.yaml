apiVersion: v1
kind: Service
metadata:
  name: my-kubernetes-service
spec:
  selector:
    app: my-web-server
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 8080
  type: LoadBalancer
